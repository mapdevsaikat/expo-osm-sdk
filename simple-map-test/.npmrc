# Optional — only needed when using a local `file:` dependency on ../expo-osm-sdk.
# `install-links=true` copies the linked package into node_modules instead of
# symlinking, so peer dependencies resolve from this app's node_modules.
# Default Play Store / EAS builds use expo-osm-sdk from npm and do not need this.
